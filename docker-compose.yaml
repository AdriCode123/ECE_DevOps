version: '3'
services:
  redis:
    image: redis
    volumes: 
      - redis-data:/data
  web:
    # COMPLETE HERE
    image: image-compose2:latest
    build: .
    ports:
      - "5000:8080"
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
volumes:
  redis-data: